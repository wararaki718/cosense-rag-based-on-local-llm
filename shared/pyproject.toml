[project]
name = "shared"
version = "0.1.0"
description = "Shared models and utilities for Scrapbox RAG"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
